#+TITLE: Static Blueprints
#+AUTHOR: Manifolding Atlas Git VC System
#+DESCRIPTION: Auto-generated - notes holding static defaults that want unfolding links.
#+STARTUP: overview

* TODO [[id:a7a350b4-60e9-445c-9da8-350c10778b87][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:700ef578-8aa0-4d28-9f6b-2b998fe15c36][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:23973f80-bbd5-432f-99f1-2b4711e2b9b6][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:6b2b9cf7-56cf-423d-a252-ab95ad1ea1f5][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:c8aae1da-b72f-424b-ab68-0ecb0b209746][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:34592c0c-7a66-44a3-9ca9-946f8ac88ff1][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:dd13f649-5522-4de6-b6dc-a0baa0ce5fd0][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:e1fa129d-3507-49d8-ab3f-76a85e1bd68b][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:f05792f2-af45-4469-8cb7-5925cce1e8cf][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:1b2da354-25af-4cc0-bb2a-e1c087323429][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:b3ff74aa-5c91-4c6c-b10e-f3617eeb94c5][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:320f8d83-20ad-49ac-8ce0-d51f4b6c255b][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:0df00db1-6b6d-48df-9726-301811406341][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:4dfc1d23-08ad-4693-ae24-74702e4744c6][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:a0b7ae5f-7c93-4368-a58b-34b9d67704cc][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:88279f30-2d9b-4ba7-a9ef-0ca97ea145f0][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:6358b8a5-28a9-4515-80aa-7768fcfd9076][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:ba24f951-3c1d-4a12-8dc4-28f05dd3caa8][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:a7226793-7d32-4680-a031-4509a493091e][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:4f7e4c69-702b-4e88-884e-abd70574f830][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:d7d32d83-a2cc-4640-b057-a7bd18bf3317][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:b8a67625-d45f-4020-9c3f-6a2dab102a71][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:837f06cb-bdfd-4a01-b0fb-aea46b2d6cf6][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:aa50bc91-94f9-4c5c-8b49-3ddec6aec6a5][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:2c492190-27d4-4d86-8698-9a7823800812][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:cce8772a-523d-4d73-bce3-7b1dfd75e0ee][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:84c7c60f-3be8-48d8-9abb-9a6a33c563b7][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:bedf7e4d-0961-49ad-9ec5-3a727d8e35f3][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:413f673c-a008-4215-b1d3-51c3388ffbb4][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:4a7c39ca-27b1-4d55-af88-5e98a82ada4a][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:e865bd65-fe3a-4a36-a86d-8a8ab435759c][t1]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:683c11ce-f99b-4177-9ff2-059bffea8f5d][t2]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:64c4b196-552a-4fc7-85c0-d42e62dbdab6][t3]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:8ab5323b-83ce-4fa5-a5f6-e78bbf0d9ab3][t4]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:ed0b91d6-4285-4348-a00b-6515bbae09eb][t5]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:4ae9fcd7-f78a-422c-a55f-54e90e7693f4][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:eaa305d8-3a57-4556-a76b-4aa28321f772][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:aec2af64-219b-44fd-8af7-e8427cda0ca7][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:b70ee04a-be89-4638-84b7-0f327814b3d5][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:76151373-7ebe-4368-b812-286776711fd2][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:2aea34b7-fb85-4fd1-a3ac-a808acf416c9][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:8c238707-23ed-438c-9663-1029e63c32fa][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:9c04beb5-b7f7-44ad-b351-ddced1b10dad][TODO]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

* TODO [[id:9dc0c16f-905a-49e7-8d30-6d83a0e5d1e0][t9]]
  :PROPERTIES:
  :CREATED:       <2026-09-12 Sat>
  :SCHEMAS:       base
  :VIOLATIONS:    Required field "STATE_WORK" is missing; Required field "STATE_GENERAL" is missing; Required field "STATE_INTELLIGENCE" is missing; Required field "STATE_OPERATIONAL" is missing; Required field "STATE_VALIDATION" is missing; Required field "STATE_SOURCE" is missing
  :END:

